import FormButton from 'webapp/components/form-button';

/**
 * Form submit button.
 */
export default FormButton.extend({
  classNames: ['form-button--submit'],
  layoutName: 'components/form-button',
  type: 'submit'
});
